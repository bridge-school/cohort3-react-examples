import React from "react";
import renderer from "react-test-renderer";
import { FriendList } from "./friend-list";

// write a test that checks that the component generates a list of friends if data is provided

// write a test for when no data is provided
